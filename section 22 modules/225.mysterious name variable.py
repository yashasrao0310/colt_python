# __name__ -> when run, every python file has a __name__variable
# if the file is the main file being run, its value is "__main__"
# Othwewise, its value is the file name

# see say_hi.py for code implementation